export default function Page() {
  return <p>Dashboard Things yeah buddy</p>
  //This is called: File Sistem Routing
  // it has to be a default function, and the name of the function is ignored,
  //However, you should always put a proper name so that if we have an error, we can pinpoint from where
}